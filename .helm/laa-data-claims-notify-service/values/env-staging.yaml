serviceAccountName: claims-notify-service-staging-irsa-serviceaccount

replicas: 1
metrics:
  mainReleaseName: staging

grafana:
  enabled: true

autoscaling:
  enabled: false

sentry:
  enabled: false
  environment: staging
  tracesSampleRate: 0.25
