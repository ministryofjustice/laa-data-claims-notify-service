services:
  notify-service:
    container_name: data-claims-notify-service
    build:
      context: .
    ports:
      - "8183:8183"
      - "5083:5083"
    environment:
      - SERVER_PORT=8083
      - SPRING_PROFILES_ACTIVE=local
      - JAVA_TOOL_OPTIONS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5083
